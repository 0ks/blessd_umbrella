<h1 class="title has-text-centered"><%= gettext("Listing Services") %></h1>

<table class="table">
  <thead>
    <tr>
      <th><%= gettext("Name") %></th>
      <th><%= gettext("Date") %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for service <- @services do %>
    <tr>
      <td><%= service.name %></td>
      <td><%= humanize(service.date) %></td>

      <td>
        <div class="field has-addons is-pulled-right">
          <div class="control">
            <%= link gettext("Edit"),
              to: service_path(@conn, :edit, service),
              class: "button is-small" %>
          </div>
          <div class="control">
            <%= link gettext("Delete"),
              to: service_path(@conn, :delete, service),
              method: :delete,
              data: [confirm: gettext("Are you sure?")],
              class: "button is-danger is-small" %>
          </div>
        </div>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link gettext("New Service"), to: service_path(@conn, :new), class: "button is-primary" %>
