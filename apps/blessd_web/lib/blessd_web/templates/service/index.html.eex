<h1 class="title has-text-centered"><%= gettext("Listing Services") %></h1>

<table class="table">
  <thead>
    <tr>
      <th><%= gettext("Date") %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for service <- @services do %>
    <tr>
      <td><%= humanize(service.date) %></td>

      <td>
        <div class="field has-addons is-pulled-right">
          <div class="control">
            <%= link gettext("Attendance"),
              to: Routes.service_attendance_path(@conn, :index, @current_user.church.identifier, service),
              class: "button is-small" %>
          </div>
          <div class="control">
            <%= link gettext("Edit"),
              to: Routes.service_path(@conn, :edit, @current_user.church.identifier, service),
              class: "button is-small" %>
          </div>
          <div class="control">
            <%= link gettext("Delete"),
              to: Routes.service_path(@conn, :delete, @current_user.church.identifier, service),
              method: :delete,
              data: [confirm: gettext("Are you sure?")],
              class: "button is-danger is-small" %>
          </div>
        </div>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link gettext("New Service"), to: Routes.service_path(@conn, :new, @current_user.church.identifier), class: "button is-primary" %>
